{
  "name": "bandcamp-scrobbler",
  "version": "1.6.0",
  "description": "Browser extension for scrobbling albums on Bandcamp to Last.fm",
  "author": "Ben-Wormald",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "build:dev:firefox": "MODE=dev BROWSER=firefox node build.js && npm run package:zip",
    "build:dev:chrome": "MODE=dev BROWSER=chrome node build.js",
    "build:firefox": "BROWSER=firefox node build.js && npm run package:xpi",
    "build:chrome": "BROWSER=chrome node build.js && npm run package:zip",
    "package:xpi": "cd dist && zip -rq ../bandcamp-scrobbler.xpi ./*",
    "package:zip": "cd dist && zip -rq ../bandcamp-scrobbler.zip ./*"
  },
  "dependencies": {
    "js-md5": "^0.8.3",
    "preact": "^10.20.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
