{
  "name": "bandcamp-scrobbler",
  "version": "1.5.1",
  "description": "Browser extension for scrobbling albums on Bandcamp to Last.fm",
  "scripts": {
    "build": "./check-env.js && webpack --mode=production",
    "build:dev": "./check-env.js && webpack --mode=development",
    "package:firefox": "cd dist && zip -r bandcamp-scrobbler.xpi ./*",
    "package:chrome": "cd dist && zip -r bandcamp-scrobbler.zip ./*",
    "build:firefox": "BROWSER=firefox npm run build && npm run package:firefox",
    "build:chrome": "BROWSER=chrome npm run build && npm run package:chrome",
    "build:dev:firefox": "BROWSER=firefox npm run build:dev && npm run package:firefox",
    "build:dev:chrome": "BROWSER=chrome npm run build:dev"
  },
  "author": "Ben-Wormald",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "js-md5": "^0.7.3",
    "preact": "^10.13.2"
  },
  "devDependencies": {
    "@babel/core": "^7.21.4",
    "babel-loader": "^9.1.2",
    "babel-preset-preact": "^2.0.0",
    "copy-webpack-plugin": "^11.0.0",
    "webpack": "^5.78.0",
    "webpack-cli": "^5.0.1"
  }
}
